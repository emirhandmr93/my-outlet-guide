import { existsSync, mkdirSync, writeFileSync } from "node:fs";
import { categories } from "../src/constants/categories";
import { cities } from "../src/constants/cities";
import { countries } from "../src/constants/countries";
import { outlets } from "../src/constants/outlets";

const web = "web";
const esc = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[char]!));
const write = (route: string, html: string) => { mkdirSync(`${web}/${route}`, { recursive: true }); writeFileSync(`${web}/${route}/index.html`, html); };
const nav = `<a href="/">Ana Sayfa</a><a href="/explore/">Keşfet</a><a href="/outlets/">Outletler</a><a href="/savings/">Tasarruf</a><a href="/trip-planner/">Seyahatlerim</a><a href="/app/">Uygulama</a>`;
const footer = `<footer><div class="footer-grid"><div><strong>MY OUTLET GUIDE</strong><p>Outlet keşfi, seyahat planlama ve alışveriş hazırlığı için uygulama odaklı rehber.</p></div><div><p class="footer-label">KEŞFET</p><a href="/explore/">Keşfet</a><a href="/outlets/">Outletler</a><a href="/countries/">Ülkeler</a><a href="/cities/">Şehirler</a></div><div><p class="footer-label">DESTEK</p><a href="/privacy/">Privacy</a><a href="/terms/">Terms</a><a href="/account-deletion/">Account Deletion</a><a href="/contact/">Contact</a></div><div><p class="footer-label">İLETİŞİM</p><a href="mailto:info@myoutletguide.com">info@myoutletguide.com</a><a href="https://instagram.com/myoutletguide">@myoutletguide</a></div></div></footer>`;
function page(title: string, eyebrow: string, h1: string, intro: string, body: string) { return `<!doctype html><html lang="tr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | My Outlet Guide</title><meta name="description" content="${esc(intro)}"><link rel="stylesheet" href="/assets/styles.css"></head><body><header><nav><a class="brand" href="/"><span class="brand-mark" aria-hidden="true"></span><span>MY OUTLET GUIDE</span></a><div class="links">${nav}</div><a class="nav-app" href="/app/">Uygulamayı incele</a></nav><section class="hero"><p class="pill">${eyebrow}</p><h1>${h1}</h1><p>${intro}</p></section></header><main>${body}</main>${footer}</body></html>`; }
const cta = `<section class="app-cta"><div><p class="eyebrow">MY OUTLET GUIDE UYGULAMASI</p><h2>Planın, rehberin ve favorilerin yanında.</h2><p>Uygulamada favorilerini sakla, seyahatini düzenle ve outlet notlarına eriş.</p></div><a class="button" href="/app/">Uygulama durumunu incele</a></section>`;
const card = (title: string, text: string, href?: string, icon = "✦") => `<article class="card">${href ? `<a class="card-link" href="${href}">` : ""}<span class="card-icon">${icon}</span><h3>${title}</h3><p>${text}</p>${href ? `<span class="card-arrow">İncele <b>→</b></span></a>` : ""}</article>`;
const cards = (items: string[], extra = "") => `<section class="grid cards ${extra}">${items.join("")}</section>`;
const outletCard = (o: any) => { const city = cities.find((item) => item.cityId === o.cityId); const country = countries.find((item) => item.countryId === o.countryId); return card(esc(o.name), `${esc(city?.cityName || o.cityId)} · ${esc(country?.countryName || o.countryId)}${o.storesCountText ? ` · ${esc(o.storesCountText)}` : ""}`, `/outlets/${o.slug || o.outletId}/`, "⌖"); };
const heroVisual = `<div class="hero-art" aria-label="My Outlet Guide uygulama kartları"><span class="art-glow"></span><span class="art-route"></span><span class="art-card art-card-one">OUTLET<br><b>ROTAM</b></span><span class="art-card art-card-two">TAX FREE<br><b>NOTLAR</b></span><span class="art-pin">⌖</span></div>`;

write("", page("Outlet keşfi ve seyahat planlama", "OUTLET KEŞFİ VE PLANLAMA", "Alışveriş seyahatin için akıllı bir başlangıç.", "Outletleri keşfet, rotanı düzenle ve tasarruf notlarını tek bir uygulama dilinde takip et.", `<section class="hero-card home-hero"><div><p class="eyebrow">AVRUPA VE ÖTESİ</p><h2>Bir sonraki outlet rotanı keşfet.</h2><p>Şehirler, outletler, Tax Free rehberi ve yolculuk hazırlığı; uygulamadaki gibi sade kartlarda.</p><p class="cta"><a class="button" href="/explore/">Outletleri keşfet</a><span class="button muted">App Store incelemesi bekleniyor</span><span class="button muted">Google Play hazırlıkta</span></p></div>${heroVisual}</section><section class="section-heading"><p class="eyebrow">BUGÜN NEREYE?</p><h2>Uygulamadaki keşif akışı, webde de yanında.</h2></section>${cards([card("Outlet keşfi", "Ülke, şehir ve outlet kartlarıyla rotana başla.", "/explore/", "⌕"), card("Seyahat planı", "Duraklarını ve hazırlık notlarını uygulamada düzenle.", "/trip-planner/", "◫"), card("Tasarruf araçları", "Bütçeni ve Tax Free ihtimallerini bilgiyle değerlendir.", "/savings/", "₺"), card("Çevrimdışı rehber", "Temel rehber bilgisinin nasıl kullanılacağını öğren.", "/offline-guide/", "✓")], "feature-grid")}${cta}`));

write("explore", page("Keşfet", "KEŞFET", "Nereye gitmek istersin?", "Ülke, şehir veya outlet adına göre uygulama tarzı bir başlangıç yap.", `<section class="explore-panel">${heroVisual}<div class="search"><label for="discover">⌕ &nbsp; Outlet, şehir veya ülke ara</label><input id="discover" type="search" readonly><div class="chips"><span>Ülkeler</span><span>Şehirler</span><span>Outletler</span></div><p>Arama ve kişiselleştirilmiş listeler uygulamada kullanılabilir.</p></div></section><section class="section-heading"><p class="eyebrow">POPÜLER ARAMALAR</p><h2>Keşfe buradan başla</h2></section>${cards(outlets.slice(0, 6).map(outletCard), "outlet-grid")}<section class="section-heading"><p class="eyebrow">DESTİNASYONLAR</p><h2>Ülke ve şehir kartları</h2></section>${cards(countries.slice(0, 12).map((country: any) => card(`${country.countryFlag} ${country.countryName}`, `${outlets.filter((outlet: any) => outlet.countryId === country.countryId).length} outlet · ${country.currency}`, `/countries/${country.countryId}/`, "◎")))}${cta}`));

write("countries", page("Ülkeler", "DESTİNASYONLAR", "Ülkelere göre keşfet", "Para birimi, Tax Free notları ve ilgili outletlerle rotanı şekillendir.", `<section class="section-heading compact"><p class="eyebrow">ÜLKE REHBERİ</p><h2>Keşif kartların hazır</h2></section>${cards(countries.map((country: any) => card(`${country.countryFlag} ${country.countryName}`, `${outlets.filter((outlet: any) => outlet.countryId === country.countryId).length} outlet · ${country.currency} · Tax Free bilgisi`, `/countries/${country.countryId}/`, "◎")))}${cta}`));
for (const country of countries as any[]) { const related = outlets.filter((outlet: any) => outlet.countryId === country.countryId); const cityList = cities.filter((city: any) => city.countryId === country.countryId); write(`countries/${country.countryId}`, page(`${country.countryName} outlet rehberi`, "ÜLKE REHBERİ", `${country.countryFlag} ${country.countryName}`, `${related.length} outlet, ${cityList.length} şehir ve ${country.currency} bilgisi.`, `<section class="facts"><div><b>${related.length}</b><span>outlet</span></div><div><b>${cityList.length}</b><span>şehir</span></div><div><b>${country.currency}</b><span>para birimi</span></div></section><section><h2>Şehirler</h2>${cards(cityList.map((city: any) => card(city.cityName, `${related.filter((outlet: any) => outlet.cityId === city.cityId).length} outlet`, `/cities/${city.cityId}/`, "⌖")))}</section><section><h2>İlgili outletler</h2>${cards(related.map(outletCard), "outlet-grid")}</section><section class="notice"><h3>Tax Free notu</h3><p>Uygunluk ve iade süreci ülke, mağaza ve sağlayıcı kurallarına göre değişir. Satın alma öncesinde güncel koşulları doğrulayın.</p></section>${cta}`)); }

write("cities", page("Şehirler", "ŞEHİRLER", "Şehir rotanı seç", "Outletlerin bulunduğu şehirleri ve yakın alışveriş duraklarını incele.", cards(cities.map((city: any) => { const country = (countries as any[]).find((item) => item.countryId === city.countryId); return card(city.cityName, `${country?.countryName || city.countryId} · ${outlets.filter((outlet: any) => outlet.cityId === city.cityId).length} outlet`, `/cities/${city.cityId}/`, "⌖"); })) + cta));
for (const city of cities as any[]) { const country = (countries as any[]).find((item) => item.countryId === city.countryId); const related = outlets.filter((outlet: any) => outlet.cityId === city.cityId); write(`cities/${city.cityId}`, page(`${city.cityName} outlet rehberi`, "ŞEHİR REHBERİ", city.cityName, `${country?.countryName || city.countryId} · ${related.length} outlet.`, `<section class="facts"><div><b>${related.length}</b><span>outlet</span></div><div><b>${esc(country?.countryName || city.countryId)}</b><span>ülke</span></div></section><section><h2>Outletler</h2>${related.length ? cards(related.map(outletCard), "outlet-grid") : '<div class="notice"><p>Bu şehir için uygulamadaki outlet rehberi güncellendikçe burada listelenir.</p></div>'}</section><section class="notice"><h3>Ulaşım notu</h3><p>Havaalanı ve şehir merkezi mesafeleri outlet detayında mevcut olduğunda gösterilir. Yolculuk öncesi güncel ulaşım bilgisini kontrol edin.</p></section>${cta}`)); }

write("outlets", page("Outletler", "OUTLET REHBERİ", "Outletleri keşfet", "Yer, çalışma saatleri, hizmetler ve seyahat notlarını aynı kart ritminde incele.", `<section class="search list-search"><label>⌕ &nbsp; Outlet, şehir veya ülke ara</label><p>Uygulamada arayarak kendi listenizi oluşturabilirsiniz.</p></section><section><h2>Tüm outletler</h2>${cards(outlets.map(outletCard), "outlet-grid")}</section>${cta}`));
for (const outlet of outlets as any[]) { const city = (cities as any[]).find((item) => item.cityId === outlet.cityId); const country = (countries as any[]).find((item) => item.countryId === outlet.countryId); const slug = outlet.slug || outlet.outletId; const image = `/assets/outlet-images/${slug}/hero.jpg`; const visual = existsSync(`assets/outlet-images/${slug}/hero.jpg`) ? `<img src="${image}" alt="${esc(outlet.name)}">` : `<div class="image-fallback"><span>⌖</span><b>OUTLET REHBERİ</b></div>`; write(`outlets/${slug}`, page(`${outlet.name} outlet rehberi`, "PREMİUM OUTLET REHBERİ", esc(outlet.name), `${city?.cityName || outlet.cityId}, ${country?.countryName || outlet.countryId}`, `<section class="detail-hero">${visual}<div><p class="eyebrow">${esc(country?.countryName || outlet.countryId)} · ${esc(city?.cityName || outlet.cityId)}</p><h2>${esc(outlet.storesCountText || "Outlet bilgileri")}</h2><p>${esc(outlet.address || "Adres bilgisi uygulamadaki rehberde yer alır.")}</p><div class="action-row"><span>♡ Favori</span><span>◫ Seyahat Oluştur</span><span>⌖ Yol Tarifi</span></div></div></section><div class="detail-tabs"><span>Özet</span><span>Markalar</span><span>Ulaşım</span><span>Yemek</span><span>Yorumlar</span></div><section class="facts"><div><b>${esc(outlet.openingHours || "Kontrol edin")}</b><span>çalışma saatleri</span></div><div><b>${outlet.taxFreeAvailable ? "Var" : "Mağazaya göre"}</b><span>Tax Free</span></div><div><b>${outlet.cityCenterDistanceKm ?? "—"} km</b><span>şehir merkezi</span></div></section><section><h2>Hizmetler</h2><div class="chips service-chips">${(outlet.services || []).slice(0, 12).map((service: string) => `<span>✓ ${esc(service)}</span>`).join("") || "<span>Hizmet bilgisi güncellendikçe eklenir.</span>"}</div></section>${outlet.restaurants?.length ? `<section><h2>Restoranlar ve kafeler</h2>${cards(outlet.restaurants.slice(0, 8).map((restaurant: string) => card(esc(restaurant), "Outlet içindeki mekan bilgisi.", undefined, "☕")))}</section>` : ""}<section class="notice"><h3>Ulaşım ve ziyaret notu</h3><p>${outlet.airports?.length ? esc(outlet.airports.map((airport: any) => `${airport.code} · ${airport.distanceKm} km`).join(" · ")) : "Havaalanı ve ulaşım bilgisi uygulamada mevcut olduğunda gösterilir."}</p>${outlet.websiteUrl ? `<p><a class="button secondary" href="${esc(outlet.websiteUrl)}">Resmî web sitesi</a>${outlet.googleMapsUrl ? ` <a class="button secondary" href="${esc(outlet.googleMapsUrl)}">Haritada aç</a>` : ""}</p>` : ""}</section>${cta}`)); }

write("brands", page("Markalar ve kategoriler", "MARKA KEŞFİ", "Alışveriş tarzını seç", "Kategori bazında aramaya uygulamada devam et.", `<section class="category-grid">${(categories as any[]).filter((item) => ["luxury", "fashion", "sportswear", "shoes-bags", "beauty", "jewelry-watches", "eyewear"].includes(item.categoryId)).map((item) => card(`${item.icon} ${item.categoryName}`, "Bu kategorideki markaları ve outletleri uygulamada keşfet.", "/app/", item.icon)).join("")}</section>${cta}`));
const prose: Record<string, [string, string, string, string]> = {
  "tax-free": ["Tax Free rehberi", "TAX FREE", "Tax Free nedir?", `<section class="tool-hero"><div><p class="eyebrow">BİLGİYLE PLANLA</p><h2>İade sürecini adım adım anla.</h2><p>Uygunluk; ikamet, ülke, mağaza katılımı, harcama tutarı, form ve gümrük doğrulamasına bağlıdır.</p></div><span>₺</span></section><section class="notice disclaimer"><h3>Önemli açıklama</h3><p>Tahminler bilgilendirme amaçlıdır; nihai iade mağaza, sağlayıcı, ülke kuralları, işlem ücretleri ve uygunluğa göre değişebilir.</p></section>`],
  savings: ["Tasarruf araçları", "TASARRUF", "Bütçeni şeffaf biçimde değerlendir", `<section class="tool-hero"><div><p class="eyebrow">AKILLI ALIŞVERİŞ</p><h2>Hesaplarını yanında taşı.</h2><p>Karşılaştırmalar kişisel planlama içindir; tasarruf garanti edilmez.</p></div><span>₺</span></section>${cards([card("Akıllı Alışveriş Hesaplayıcısı", "Liste fiyatı, indirim ve olası maliyetleri kendi girdilerinle değerlendir.", "/app/", "⌘"), card("Fiyat Avantajı", "Fiyatları planlama bağlamında karşılaştır.", "/app/", "↗"), card("Tax Free Hesaplayıcı", "Uygunluk koşullarını dikkate alarak tahminleri incele.", "/tax-free/", "₺")])}`],
  "trip-planner": ["Seyahat planı", "SEYAHATLERİM", "Alışveriş rotanı uygulamada oluştur", `<section class="trip-card"><div class="route-line"><span>İ</span><i></i><span>O</span></div><div><p class="eyebrow">ROTA ÖNİZLEMESİ</p><h2>Şehirden outlete, tek plan.</h2><p>Tarihlerini kaydet, outlet ekle ve hazırlık hatırlatmalarını uygulamada düzenle.</p></div></section>${cards([card("Tarih ve durum", "Seyahat tarihlerini ve plan durumunu kolayca takip et.", "/app/", "▣"), card("Hazırlık planı", "Tax Free belgeleri ve kişisel notların için kontrol listesi oluştur.", "/app/", "✓")])}<section class="notice"><p>Web üzerinden seyahat oluşturma sunulmaz; bu özellik uygulama odaklıdır.</p></section>`],
  "flight-deals": ["Uçuş uyarıları", "UÇUŞ UYARILARI", "Rotanı şimdiden seç", `<section class="tool-hero"><div><p class="eyebrow">ROTA TERCİHİ</p><h2>Uyarıların için hazırlık yap.</h2><p>Uçuş fırsatı tercihleri, desteklenen sağlayıcı ve rotalar hazır olduğunda uygulamada kullanılacaktır.</p></div><span>✈</span></section><section class="preference-card"><p class="eyebrow">ÖNİZLEME</p><div><span>Kalkış şehri</span><span>Varış şehri</span><span>Esnek tarihler</span></div><p>Uçuş fiyat sağlayıcısı bağlandığında desteklenen rotalar için uyarılar hazırlanacaktır.</p><small>Bu sayfada canlı fiyat, bilet satışı veya rezervasyon sunulmaz.</small></section>`],
  "offline-guide": ["Çevrimdışı rehber", "ÇEVRİMDIŞI REHBER", "Temel rehber bilgisi yanında", `<section class="tool-hero"><div><p class="eyebrow">YOLCULUKTA</p><h2>Temel bilgileri yanında tut.</h2><p>Yüklenen rehber kapsamına göre outlet detayları, ülkeler ve mevcut notlar uygulamada incelenebilir.</p></div><span>↓</span></section>${cards([card("Outlet detayları", "Kaydedilmiş rehber bilgilerinin kapsamını uygulamada gör.", "/app/", "⌖"), card("Ülke ve marka notları", "Yolculuk için temel keşif bilgilerini yanında bulundur.", "/app/", "✓")])}<section class="notice"><p>Çevrimdışı kullanılabilir içerik kapsamı cihaza ve indirilen rehber verisine göre değişir. Tam eşzamanlılık veya canlı verinin çevrimdışı kullanılabilirliği vaat edilmez.</p></section>`],
  app: ["Uygulama", "UYGULAMA DURUMU", "My Outlet Guide uygulaması", `<section class="tool-hero"><div><p class="eyebrow">MOBİL DENEYİM</p><h2>Keşfet, planla, yanında taşı.</h2><p>Outlet keşfi, seyahat planlama ve tasarruf araçları uygulama odaklı olarak geliştirilir.</p></div><span>✦</span></section>${cards([card("iOS", "App Store incelemesi bekleniyor. TestFlight test edildi.", undefined, "●"), card("Android", "Google Play hazırlıkta; doğrulama süreci sürüyor.", undefined, "●"), card("Destek", "Yardım için info@myoutletguide.com adresine yazın.", "/contact/", "? ")])}`]
};
for (const [slug, [title, eyebrow, heading, body]] of Object.entries(prose)) write(slug, page(title, eyebrow, heading, "Outlet keşfi, seyahat planlama ve tasarruf araçları için uygulama durumu.", body + cta));
