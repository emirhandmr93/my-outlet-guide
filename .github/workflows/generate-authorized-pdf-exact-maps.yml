name: Generate authorized vector-plan exact maps

on:
  push:
    branches:
      - feat/premium-map-exact-completion
    paths:
      - tools/generateAuthorizedPdfExactMaps.ts
      - tools/checkPremiumOutletMaps.ts
      - src/features/premiumOutletMaps/exactCatalog.ts
      - src/features/premiumOutletMaps/generatedPdfExactMaps.ts
      - src/features/premiumOutletMaps/types.ts
      - src/features/premiumOutletMaps/authorizedSourceManifest.ts
      - docs/premium-map-pdf-evidence/**
      - docs/PREMIUM_MAP_PDF_IMPORT_AUDIT.json
      - .github/workflows/generate-authorized-pdf-exact-maps.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: generated-pdf-exact-maps-${{ github.ref }}
  cancel-in-progress: true

jobs:
  generate-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate exact vector-plan maps
        run: npx tsx tools/generateAuthorizedPdfExactMaps.ts

      - name: Validate generated vector-plan coverage
        run: |
          test -f docs/PREMIUM_MAP_PDF_GENERATION_REPORT.json
          node - <<'NODE'
          const fs = require('fs');
          const report = JSON.parse(fs.readFileSync('docs/PREMIUM_MAP_PDF_GENERATION_REPORT.json', 'utf8'));
          if (report.summary.exactMapCount !== 2) throw new Error(`Expected 2 generated vector-plan maps, got ${report.summary.exactMapCount}`);
          for (const outlet of report.results) {
            if (outlet.canonicalCoverage < 0.75) throw new Error(`${outlet.outletId}: canonical coverage below 75% (${outlet.canonicalCoverage})`);
            if (outlet.exactPointStoreCount < 20) throw new Error(`${outlet.outletId}: too few exact point stores`);
          }
          NODE

      - name: Validate premium map release gate
        run: npm run validate:premium-maps

      - name: Typecheck generated snapshot
        run: npm run typecheck

      - name: Commit generated vector-plan maps
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/premiumOutletMaps/generatedPdfExactMaps.ts docs/PREMIUM_MAP_PDF_GENERATION_REPORT.json
          if git diff --cached --quiet; then
            echo "No generated vector-plan changes to commit."
            exit 0
          fi
          git commit -m "Generate authorized exact vector-plan premium maps"
          git pull --rebase origin "${{ github.ref_name }}"
          git push origin HEAD:${{ github.ref_name }}
