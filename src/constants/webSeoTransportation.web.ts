// Every published outlet currently has an indexable transportation page. Build-time
// tooling verifies that invariant against the authoritative synchronous dataset.
export function hasWebSeoTransportation(_outletId: string) {
  return true;
}
