# Generated input sources deprecated

Generated/AI/generic outlet media imports are disabled. Do not add source images or manifests that import from this folder.

Historical planning files may remain for audit context only, but tooling now rejects manual/local source paths from `media-sources/generated-inputs/`. Exact manual outlet photos, when explicitly allowed by the media policy, must use `media-sources/manual-inputs/` instead.
